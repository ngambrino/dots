# dots

My dotfiles & other config stuff.


## Prerequisites

* Brew
* Git `brew install git`


## Installation

* `git clone https://github.com/ngambrino/dots.git && cd dots`
* `brew bundle --no-lock --no-upgrade`
* `./setup`
